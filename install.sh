echo "
# Add Neovim to the path
export PATH=\"\$PATH:/opt/nvim/\"
alias vi=\"nvim\"
alias vim=\"nvim\"" >> ~/.bashrc

# Install NerdFonts. add them to /usr/share/fonts
# then set the font to be used by a terminal profile. 
# Then set the terminal profile to use them. 
